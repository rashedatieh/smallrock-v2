# docs/

Media used by the main README.

- **`cover.png`** — the repository cover image shown at the top of the root README.

To swap in an animated demo, record a short clip of Small Rock in action, save it as `demo.gif` here, and point the README `<img src>` at it.
